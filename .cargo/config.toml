# KinLab Cargo registry — packages published here are served by kin-daemon.
# This file is NOT gitignored — all builds (local + CI) use the Kin registry.

[registries.kin]
index = "sparse+https://kinlab.ai/registry/cargo/"
