# Patch coverage is informational, not a merge gate. Some new lines are runtime
# embedding-dispatch glue that only executes with a live model or GPU backend and
# cannot be exercised by the unit suite; their decision logic is unit-tested
# separately. `cargo test --workspace` and project coverage remain the gate.
coverage:
  status:
    patch:
      default:
        informational: true
